// UI は本フェーズでは未実装(BFF / API 実装フェーズ)。
// 画面(曜日タブ・時刻グリッド・初期化ダイアログ)は implementation/005・006 で実装する。
export default function Home() {
  return (
    <main>
      <h1>タイムアナウンスメント 設定</h1>
      <p>UI は未実装です。現在は BFF(/api/schedules)の実装フェーズです。</p>
    </main>
  );
}
